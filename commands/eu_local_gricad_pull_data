#!/bin/bash

# set default paths for GRICAD cluster
EXTERNAL_EXPERIMENTS_PATH=$EU_PRJ_GRICAD_EXPERIMENTS_PATH
EXTERNAL_MACHINE=$EU_PRJ_GRICAD_DEFAULT_DATA_TRANSFER_MACHINE

eu_local_external_pull_data -s "$EXTERNAL_EXPERIMENTS_PATH" -m "$EXTERNAL_MACHINE" "$@"