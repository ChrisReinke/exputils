def get_config():

    config = dict()

    config['seed'] = 468413 + <repetition_id>

    config['n_steps'] = <n_steps>

    config['init_value'] = <init_value>
    config['force'] = <force>
    config['sigma'] = <sigma>

    return config
